<!DOCTYPE html>
<!-- paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither/ -->
<!--[if lt IE 7 ]> <html class="ie6" lang="en"> <![endif]-->
<!--[if IE 7 ]>    <html class="ie7" lang="en"> <![endif]-->
<!--[if IE 8 ]>    <html class="ie8" lang="en"> <![endif]-->
<!--[if (gte IE 9)|!(IE)]><!--> <html lang="en"> <!--<![endif]-->
  <head>
    <meta charset="utf-8" />

    <!-- Uncomment to make IE8 render like IE7 -->
    <!-- <meta http-equiv="X-UA-Compatible" content="IE=7" /> -->

    <!-- Set the viewport width to device width for mobile -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Lerdehrberd" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/custom.modernizr" %>
    <%= csrf_meta_tags %>
  </head> 

  <body>
    <div class="row center">
      <div class="small-12 columns">
        <% flash.each do |name, msg| -%>
          <%= content_tag :div, msg, class: name %>
        <% end -%>
      </div>
    </div>

    <header>
	    <div class="row">
	      <div class="small-12 columns">
	        <h1 class="center">Lerdehrberd</h1>
	      </div>
	    </div>
		  <nav class="row">
		    <div class="large-4 large-centered columns">
		      <form class="custom">
		        <label for="customDropdown"><h2>Serect Ah Game</h2></label>
		        <select id="customDropdown">
		          <% @game_boards.each do |game_board| %>
		            <option value="<%= game_board_path(game_board.id) %>" <%= "selected" if @game_board.id == game_board.id %>><%= link_to game_board.title, game_board_path(game_board.id) %></option>
		          <% end %>
		        </select>
		      </form>
		    </div>
		  </nav>
		</header>

    <%= yield %>

    <%= javascript_include_tag "application" %>
  </body>
</html>
